name: test_workflow😃
on:
  pull-request:
  push:
  schedule:
    - cron:'0 0 * * 5'

jobs:
  build-job:
    runs-on: ubuntu-latest
    steps:
      - users: actions/checkout@v4
      - run: ./compile_with_docker.sh